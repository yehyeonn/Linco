<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
        PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.lec.spring.repository.VenueRepository">

    <sql id="select_venue">
        select id,
               venue_name,
               address,
               limit_num,
               venue_category,
               info_tel,
               price,
               posible_start_date,
               posible_end_date,
               open_time,
               close_time,
               img
        from VENUE
        where 1 = 1;
    </sql>

    <select id="findAll" resultType="com.lec.spring.domain.VenueRepository">
        <include refid="select_venue"/>
        order by id desc;
    </select>

    <select id="findByCategory" resultType="com.lec.spring.domain.Venue">
        <include refid="select_venue"/>
        and venue_category=#{venue_category}
    </select>
</mapper>